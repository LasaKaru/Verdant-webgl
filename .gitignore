# dependencies
node_modules/
# runtime artifacts
leaderboard.json
test_lb.json
*.log

# transfer artifacts
*.bundle

# build artifact
verdant-webgl.bundle
*.bundle

# vendored engine (offline fallback, fetched via npm)
js/lib/babylon.js
